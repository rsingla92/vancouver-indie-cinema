# Server-only values. Never prefix secrets with NEXT_PUBLIC_.

# Postgres connection string (Supabase → Project Settings → Database).
# The web app reads from it; the worker writes to it. Leave empty to browse demo listings.
DATABASE_URL=

# TMDB API read access token (v4 bearer). Worker only.
TMDB_API_TOKEN=
