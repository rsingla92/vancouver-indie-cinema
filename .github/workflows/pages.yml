name: Deploy site

on:
  push:
    branches: [main]

jobs:
  deploy:
    uses: ./.github/workflows/deploy-pages.yml
    secrets: inherit
    permissions:
      contents: read
      pages: write
      id-token: write
